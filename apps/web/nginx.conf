server {
    listen ${PORT};
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing (react-router) needs every
    # unknown path to resolve to index.html, not a 404.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
